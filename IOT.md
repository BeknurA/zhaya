# 🚀 Инструкция по настройке IoT системы для Жая

## 📋 Содержание

1. [Установка зависимостей](#1-установка-зависимостей)
2. [Запуск MQTT брокера](#2-запуск-mqtt-брокера)
3. [Настройка конфигурации](#3-настройка-конфигурации)
4. [Запуск симулятора](#4-запуск-симулятора)
5. [Запуск Streamlit](#5-запуск-streamlit)
6. [Интеграция в app.py](#6-интеграция-в-apppy)
7. [Устранение неполадок](#7-устранение-неполадок)

---

## 1. Установка зависимостей

### Обновите `requirements.txt`:

```txt
streamlit
plotly
scikit-learn
openpyxl
pyarrow
supabase
psycopg2-binary
cryptography
python-dotenv
paho-mqtt
```

### Установите зависимости:

```bash
pip install -r requirements.txt
```

---

## 2. Запуск MQTT брокера

### Вариант 1: Docker (Рекомендуется)

```bash
docker run -it -p 1883:1883 -p 9001:9001 eclipse-mosquitto
```
docker run -it -p 1883:1883 -p 9001:9001 eclipse-mosquitto
### Вариант 2: Локальная установка Mosquitto

**Windows:**
```bash
# Скачать: https://mosquitto.org/download/
# После установки:
mosquitto -c mosquitto.conf
```

**Linux/Mac:**
```bash
sudo apt-get install mosquitto mosquitto-clients
sudo systemctl start mosquitto
```

### Проверка работы брокера:

```bash
# Терминал 1 - подписка
mosquitto_sub -h localhost -t "zhaya/#" -v

# Терминал 2 - публикация
mosquitto_pub -h localhost -t "zhaya/test" -m "Hello MQTT"
```

Если сообщение пришло в первом терминале — брокер работает! ✅

---

## 3. Настройка конфигурации

### Создайте конфигурационный файл для Mosquitto

**mosquitto.conf:**
```conf
# Разрешить анонимные подключения (только для разработки!)
allow_anonymous true

# Слушать на всех интерфейсах
listener 1883
protocol mqtt

# WebSocket поддержка (опционально)
listener 9001
protocol websockets

# Логирование
log_dest stdout
log_type all
```

### Перезапустите брокер с конфигурацией:

```bash
docker run -it -p 1883:1883 -p 9001:9001 -v $(pwd)/mosquitto.conf:/mosquitto/config/mosquitto.conf eclipse-mosquitto
```

---

## 4. Запуск симулятора

### Структура файлов:

```
zhaya/
├── mqtt_client.py          # IoT симулятор
├── app.py                  # Главное приложение
├── pages/
│   ├── iot_monitoring.py   # Страница мониторинга
│   └── ...
├── database_supabase.py    # Работа с БД
└── requirements.txt
```

### Запуск симулятора:

```bash
python mqtt_client.py
```

**Вывод:**
```
============================================================
🐎 MQTT IoT Симулятор для производства Жая
============================================================
🔌 Подключение к MQTT брокеру localhost:1883...
✅ Подключено к MQTT брокеру: localhost:1883
📡 Подписка на топик: zhaya/actuators/commands
🚀 Запуск симуляции для партии ID: 1
⏱️ Интервал публикации: 5 сек
📊 Начальная стадия: посол
------------------------------------------------------------

🔹 Цикл #1 | Стадия: посол
💾 Сохранено в БД: temperature = 1.234 °C
💾 Сохранено в БД: humidity = 78.5 %
💾 Сохранено в БД: ph = 6.42 pH
...
```

### Остановка симулятора:

Нажмите `Ctrl+C`

---

## 5. Запуск Streamlit

### В отдельном терминале:

```bash
streamlit run app.py
```

### Откройте браузер:

```
http://localhost:8501
```

### Проверьте работу:

1. Войдите в систему
2. Перейдите в **📡 IoT Мониторинг**
3. Выберите партию ID = 1
4. Включите **🔄 Автообновление**
5. Наблюдайте данные в реальном времени!

---

## 6. Интеграция в app.py

### Добавьте импорт страницы:

```python
# В начало файла app.py
from pages.iot_monitoring import show_iot_monitoring
```

### Добавьте пункт меню:

```python
# В секцию навигации (около строки 110)
# IoT мониторинг (доступен всем)
page_options.append(("📡 IoT Мониторинг", "iot_monitoring"))
```

### Добавьте роутинг:

```python
# В секцию роутинга (около строки 200)
elif page == "iot_monitoring":
    show_iot_monitoring(lang_choice)
```

### Полный код изменений:

```python
# === ИМПОРТЫ (строка ~10) ===
from pages.iot_monitoring import show_iot_monitoring

# === НАВИГАЦИЯ (строка ~110) ===
# После dashboard
page_options.append(("📡 IoT Мониторинг", "iot_monitoring"))

# === РОУТИНГ (строка ~200) ===
elif page == "iot_monitoring":
    show_iot_monitoring(lang_choice)
```

---

## 7. Устранение неполадок

### ❌ Проблема: "Не удалось подключиться к MQTT брокеру"

**Решение:**
1. Проверьте, запущен ли Docker/Mosquitto:
   ```bash
   docker ps
   # Должен быть контейнер eclipse-mosquitto
   ```

2. Проверьте порт 1883:
   ```bash
   netstat -an | grep 1883
   # Должно быть LISTENING
   ```

3. Измените конфиг в `mqtt_client.py`:
   ```python
   MQTT_BROKER = "localhost"  # или "127.0.0.1"
   MQTT_PORT = 1883
   ```

---

### ❌ Проблема: "Нет данных для партии ID: 1"

**Решение:**
1. Запустите симулятор:
   ```bash
   python mqtt_client.py
   ```

2. Проверьте БД напрямую:
   - Зайдите в Supabase Dashboard
   - Откройте таблицу `iot_sensor_data`
   - Должны быть записи с `batch_id = 1`

3. Проверьте логи симулятора:
   ```
   💾 Сохранено в БД: temperature = 2.5 °C
   ```
   Если этого нет — проблема с БД подключением

---

### ❌ Проблема: "Invalid comparison between dtype=datetime64[ns, UTC] and datetime"

**Уже исправлено!** Обновите `pages/iot_monitoring.py`:

```python
# Строка ~90
df['time'] = pd.to_datetime(df['time'], utc=True)

# Строка ~100
cutoff_time = pd.Timestamp.now(tz='UTC') - timedelta(minutes=minutes_ago)
```

---

### ❌ Проблема: "[WinError 10054] Удаленный хост принудительно разорвал"

**Причина:** Превышение лимита подключений к Supabase или сетевая проблема

**Решение:**
1. Добавьте retry логику в `database_supabase.py`:
   ```python
   import time
   
   def init_supabase(max_retries=3):
       for attempt in range(max_retries):
           try:
               supabase = create_client(SUPABASE_URL, SUPABASE_KEY)
               # Проверка подключения
               supabase.table('production_batches').select('count').limit(1).execute()
               return supabase
           except Exception as e:
               if attempt < max_retries - 1:
                   time.sleep(1)
                   continue
               st.error(f"Ошибка подключения: {e}")
               return None
   ```

2. Используйте connection pooling (для продакшена):
   ```python
   from supabase import create_client, ClientOptions
   
   options = ClientOptions(
       postgrest_client_timeout=10,
       storage_client_timeout=10
   )
   supabase = create_client(url, key, options)
   ```

---

## 📊 Архитектура системы

```
┌─────────────────┐
│  IoT Simulator  │ (mqtt_client.py)
│  (Python)       │
└────────┬────────┘
         │ Генерирует данные
         │ каждые 5 секунд
         ↓
┌─────────────────┐
│  MQTT Broker    │ (Eclipse Mosquitto)
│  localhost:1883 │
└────────┬────────┘
         │ Топики:
         │ • zhaya/sensors/data
         │ • zhaya/actuators/commands
         ↓
┌─────────────────┐
│   Supabase DB   │ (PostgreSQL)
│  • iot_sensor_data
│  • actuator_logs
└────────┬────────┘
         │ SQL запросы
         ↓
┌─────────────────┐
│   Streamlit     │ (app.py)
│  IoT Dashboard  │
└─────────────────┘
```

---

## ✅ Проверочный чек-лист

- [ ] MQTT брокер запущен (порт 1883)
- [ ] Суп abase подключение работает
- [ ] Симулятор запущен и отправляет данные
- [ ] В БД есть записи в `iot_sensor_data`
- [ ] Streamlit запущен (localhost:8501)
- [ ] Страница IoT Мониторинга отображается
- [ ] Графики обновляются в реальном времени
- [ ] Команды актуаторам сохраняются в БД

---

## 🎯 Следующие шаги

1. **Добавить алерты:**
   - Уведомления при выходе параметров за пределы нормы
   - Email/SMS оповещения

2. **Машинное обучение:**
   - Предсказание pH на основе истории
   - Аномальная детекция

3. **Расширенная аналитика:**
   - Корреляция между параметрами
   - Оптимизация процесса

4. **Интеграция с реальным оборудованием:**
   - Замена симулятора на реальные датчики
   - Протоколы Modbus/OPC-UA

---

## 📚 Полезные ресурсы

- [MQTT документация](https://mqtt.org/)
- [Eclipse Mosquitto](https://mosquitto.org/)
- [Paho MQTT Python](https://www.eclipse.org/paho/index.php?page=clients/python/index.php)
- [Supabase Real-time](https://supabase.com/docs/guides/realtime)

---

**Успехов! 🐎🚀**